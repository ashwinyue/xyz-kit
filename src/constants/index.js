/**
 * 应用常量配置
 */

export const APP_NAME = 'xyz';

export const KEYBOARD_SHORTCUTS = {
  HIDE_WINDOW: 'Escape',
  PROCESS_TEXT: 'Cmd+Enter',
};

export const WINDOW_CONFIG = {
  BLUR_AUTO_HIDE: true,
};
